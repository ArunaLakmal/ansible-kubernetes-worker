- name: Install required libraries
  apt:
    name: '{{ apt_libraries }}'
    state: present
  register: status

- name: Install required pip libraries
  pip:
    name: '{{ pip_packages }}'
    state: present
